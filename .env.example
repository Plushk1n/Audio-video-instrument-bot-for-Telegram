# Скопируйте этот файл в .env (в той же папке, что и скрипт бота)
# и вставьте свой токен от @BotFather вместо плейсхолдера.
#
#   cp .env.example .env
#
# Файл .env содержит секрет и НЕ должен попадать в git — он уже добавлен в .gitignore.

BOT_TOKEN=ВСТАВЬТЕ_СЮДА_НОВЫЙ_ТОКЕН
